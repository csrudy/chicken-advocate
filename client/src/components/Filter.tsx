import * as React from 'react';
import Button from './Button';

const Filter = (props) => { 

  return (
    <div>
      <Button />
      <Button />
      <Button />
      <Button />
    </div>
  )
}
export default Filter;